excluded:
  - .build

disabled_rules:
  - function_body_length
  - type_body_length
  - file_length
  - cyclomatic_complexity

trailing_comma:
  mandatory_comma: true

identifier_name:
  excluded:
    - op
    - id
