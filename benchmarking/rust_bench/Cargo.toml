[package]
name = "rust_bench"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
mcap = { path = "../../rust/mcap" }
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
# Line tables make perf/hotspot profiles readable at no runtime cost.
debug = "line-tables-only"
