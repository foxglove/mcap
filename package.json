{
  "private": true,
  "workspaces": {
    "packages": [
      "typescript"
    ]
  },
  "scripts": {
    "lint:docs": "prettier docs --check",
    "test:conformance": "yarn workspace @foxglove/mcap generate-test-data --test-dir $(pwd)/testdata/fixtures"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
