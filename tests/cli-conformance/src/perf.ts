console.error(
  "CLI performance conformance is intentionally report-only and has not been wired into CI yet. " +
    "Use the correctness runner while the initial Go-vs-Rust parity framework is being established.",
);
